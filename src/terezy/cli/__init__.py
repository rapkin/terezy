"""A thin, scriptable client over ``terezy.api`` with JSON output.

Thin is a requirement, not a description: the predecessor's CLI carried
orchestration logic that belonged in the core (REWRITE_BRIEF.md §3.2). This one
parses arguments, calls the API, and prints.
"""
