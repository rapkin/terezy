# Paths under web/src permitted to name a category id (FR-015, SC-012). One per line, with a
# reason. What may never be listed is a module under /data/: that is the generic browser, and an
# exception there is the per-category branch this check exists to forbid.

src/routes/series-map.ts
# FR-015a's two named series routes. A chart is not a generic rendering -- it needs to know it
# has a date axis and a numeric axis, and which series it is drawing -- so the two ids are
# written down here, in one file, and a third per-category screen is a decision taken in review
# rather than a file somebody adds.

src/answer/endpoints.ts
# 026 FR-006 and FR-008: the answer screen names `questions` and `instruments` because it reads
# two endpoints rather than browsing two categories -- the declared question it answers, and the
# instrument behind each member, whose read is where a kind may come from. The module holds the
# two literals and nothing else, so this exception covers them and not every query the client
# makes.

src/card/components/CardRoute.tsx
src/card/components/WhyLink.tsx
# 027 FR-009 and FR-028: the card's route path mirrors the endpoint it reads —
# `/questions/{id}/candidates/{key}` — so both modules name `questions` as a path segment of an
# endpoint under the question that published the key, not as a category to browse. Same reason as
# `endpoints.ts` above; two modules rather than one because the route declares the path and the
# link navigates to it, and a shared constant would lose the literal the router types `to` from.
